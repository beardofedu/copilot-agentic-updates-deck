name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Run linter
        run: echo "✓ Lint passed (demo stub)"
      
      - name: Run tests
        run: echo "✓ Tests passed (demo stub)"
      
      - name: Security scan
        run: echo "✓ Security scan passed (demo stub)"
